@model IEnumerable<SPH_TDS.Models.Turno>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TURDESCRICAO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TURDESCRICAO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TURID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TURID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TURID })
        </td>
    </tr>
}

</table>
